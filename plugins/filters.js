// import Vue from 'vue'

// import moment from 'moment'

// moment.locale('en')

// export function date (date) {
//   return date ? moment(date).fromNow() : '---'
// }

// export function emptyZero (value) {
//   return !!value ? value : ''
// }

// Vue.filter('date', date)
// Vue.filter('emptyZero', emptyZero)
